# hypridle.conf
# Freeze mpvpaper when idle to save GPU; resume on input.

general {
  # no lock integration here
}

listener {
  timeout = 30
  on-timeout = pkill -STOP -x mpvpaper
  on-resume  = pkill -CONT -x mpvpaper
}
